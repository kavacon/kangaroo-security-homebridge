{
  "pluginAlias": "KangarooSecurity",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "name",
        "type": "string",
        "required": true,
        "default": "KangarooSecurity"
      },
      "refreshToken": {
        "title": "the refresh token used by your kangaroo account for oauth",
        "type": "string",
        "required": true,
        "default": "xxxxxxxxxx"
      },
      "secureTokenKey": {
        "title": "the key to make secure token requests",
        "type": "string",
        "required": true,
        "default": "xxxxxxxxxx"
      }
    }
  }
}